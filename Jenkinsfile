pipeline {
    agent any

    stages {
        /*Stage 1: Build - Dependencies are now in the workspace*/
        stage('Build') {
            steps {
                sh '''
                    echo "Hello"
                '''

            }
        }

    }
}
